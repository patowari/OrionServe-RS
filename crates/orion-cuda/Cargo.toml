[package]
name = "orion-cuda"
description = "CUDA backend and custom Transformer kernels."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
orion-core = { workspace = true }
tracing = { workspace = true }

[features]
default = []
# Enables the CUDA backend. Requires a CUDA toolkit and an NVIDIA GPU.
# NOT VERIFIED: this feature has never been built or run.
cuda = []
